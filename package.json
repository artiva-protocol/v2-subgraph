{
  "name": "artivav2-staging",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:staging": "graph deploy --node https://api.thegraph.com/deploy/ neokry/artiva-v2-staging",
    "deploy:dev": "graph deploy --node https://api.thegraph.com/deploy/ neokry/artiva-v2-dev",
    "create-local": "graph create --node http://localhost:8020/ neokry/artivav2-staging",
    "remove-local": "graph remove --node http://localhost:8020/ neokry/artivav2-staging",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 neokry/artivav2-staging",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
